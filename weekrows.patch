diff --git a/README.md b/README.md
index 2a6aad3..5d4254d 100644
--- a/README.md
+++ b/README.md
@@ -25,3 +25,6 @@ pnpm install
 pnpm tauri dev
 ```
 
+## 本修改版新增
+
+- 月历左侧可显示每一行的周数，并可在「设置 → 日历内容」中选择包含 1 月 1 日的首周从 W0 或 W1 开始编号。
diff --git a/WEEK_NUMBER_MODIFICATION.md b/WEEK_NUMBER_MODIFICATION.md
new file mode 100644
index 0000000..b64ffae
--- /dev/null
+++ b/WEEK_NUMBER_MODIFICATION.md
@@ -0,0 +1,15 @@
+# 周数列功能修改说明
+
+本版本将原先“标题显示当前周数”的实现改为“月历左侧逐行显示周数”。
+
+## 功能
+
+- 月历每一行左侧显示 `W0 / W1 / W2 ...` 或 `W1 / W2 / W3 ...`。
+- 周一为每周第一天，与现有月历“周一至周日”的行结构一致。
+- 包含 **1 月 1 日** 的整行视为新年度第一周。
+- 在“设置 → 日历内容”中可以：
+  - 开关“显示每周周数”；
+  - 选择“1月1日所在周”为“第0周”或“第1周”。
+- 跨年行如果包含下一年的 1 月 1 日，会直接按新年度重新编号。
+
+例如，若选择“第1周”，则包含 1 月 1 日的那一行显示 `W1`；下一行显示 `W2`。若选择“第0周”，则分别显示 `W0`、`W1`。
diff --git a/src/components/calendar/subs/CalendarMonthGrid.tsx b/src/components/calendar/subs/CalendarMonthGrid.tsx
index 4835070..619a3da 100644
--- a/src/components/calendar/subs/CalendarMonthGrid.tsx
+++ b/src/components/calendar/subs/CalendarMonthGrid.tsx
@@ -1,36 +1,56 @@
 import { Tooltip } from 'antd';
 import classNames from 'classnames';
 import type { Dayjs } from 'dayjs';
-import type { ReactElement } from 'react';
+import { Fragment, type ReactElement } from 'react';
 import { useCalendarViewContext } from '../../../hooks/calender/CalendarViewContext.tsx';
 import type { CalendarViewClassNames } from '../../../styles/useCalendarViewStyles.ts';
 import type { CalendarCellViewModel } from '../../../utils/calendar/calendarCellModel.ts';
 import { weekdays } from '../../../utils/calendar/calendarFestivals.ts';
+import {
+  getCalendarRowWeekNumber,
+  type CalendarWeekNumberStart,
+} from '../../../utils/calendar/calendarWeekNumber.ts';
 
 export interface CalendarMonthGridProps {
   /** antd-style 生成的 className 映射 */
   styles: CalendarViewClassNames;
   /** 已由逻辑层算好的 42 格展示模型 */
   cellModels: CalendarCellViewModel[];
+  /** 是否在每一行左侧显示周数 */
+  showWeekNumbers: boolean;
+  /** 包含 1 月 1 日的第一周从 0 还是 1 开始编号 */
+  firstWeekNumber: CalendarWeekNumberStart;
   /** 用户点击某一格时回传该格公历日期 */
   onSelectDate: (date: Dayjs) => void;
 }
 
 /**
- * 月历主体：星期表头 + 日期格子（数据来自 `CalendarViewContext`）。
+ * 月历主体：周数列（可选）+ 星期表头 + 日期格子（数据来自 `CalendarViewContext`）。
  */
 function CalendarMonthGrid(): ReactElement {
   const { gridProps } = useCalendarViewContext();
-  const { styles, cellModels, onSelectDate } = gridProps;
+  const { styles, cellModels, showWeekNumbers, firstWeekNumber, onSelectDate } = gridProps;
+  const weekRows = Array.from({ length: 6 }, (_, rowIndex) => {
+    const firstCell = cellModels[rowIndex * 7];
+    return firstCell
+      ? getCalendarRowWeekNumber(firstCell.date, firstWeekNumber)
+      : firstWeekNumber;
+  });
 
   return (
-    <div className={styles.calendarGrid}>
+    <div
+      className={classNames(styles.calendarGrid, {
+        [styles.calendarGridWithWeekNumbers]: showWeekNumbers,
+      })}
+    >
+      {showWeekNumbers && <div className={styles.weekNumberHeader}>周</div>}
       {weekdays.map((day) => (
         <div className={styles.weekday} key={day}>
           {day}
         </div>
       ))}
-      {cellModels.map((cell) => {
+
+      {cellModels.map((cell, index) => {
         /** 「休」「班」角标对应不同背景色 class */
         const badgeClass =
           cell.badgeVariant === 'rest'
@@ -38,27 +58,36 @@ function CalendarMonthGrid(): ReactElement {
             : cell.badgeVariant === 'work'
               ? styles.tagWork
               : '';
+        const rowIndex = Math.floor(index / 7);
+        const isFirstCellOfRow = index % 7 === 0;
 
         return (
-          <Tooltip key={cell.dateKey} title={cell.tooltipTitle} mouseEnterDelay={0.5}>
-            <button
-              type="button"
-              className={classNames(styles.cell, {
-                [styles.otherMonth]: cell.isOtherMonth,
-                [styles.today]: cell.isToday,
-                [styles.selected]: cell.isSelected && !cell.isToday,
-              })}
-              onClick={() => onSelectDate(cell.date)}
-            >
-              {cell.badgeText && (
-                <span className={classNames(styles.tag, badgeClass)}>{cell.badgeText}</span>
-              )}
-              <span className={styles.dateText}>{cell.date.date()}</span>
-              <span className={classNames(styles.lunar, { [styles.term]: cell.hasJieQi })}>
-                {cell.displayText}
-              </span>
-            </button>
-          </Tooltip>
+          <Fragment key={`row-cell-${cell.dateKey}`}>
+            {showWeekNumbers && isFirstCellOfRow && (
+              <div className={styles.weekNumberCell} key={`week-${cell.dateKey}`}>
+                W{weekRows[rowIndex]}
+              </div>
+            )}
+            <Tooltip key={cell.dateKey} title={cell.tooltipTitle} mouseEnterDelay={0.5}>
+              <button
+                type="button"
+                className={classNames(styles.cell, {
+                  [styles.otherMonth]: cell.isOtherMonth,
+                  [styles.today]: cell.isToday,
+                  [styles.selected]: cell.isSelected && !cell.isToday,
+                })}
+                onClick={() => onSelectDate(cell.date)}
+              >
+                {cell.badgeText && (
+                  <span className={classNames(styles.tag, badgeClass)}>{cell.badgeText}</span>
+                )}
+                <span className={styles.dateText}>{cell.date.date()}</span>
+                <span className={classNames(styles.lunar, { [styles.term]: cell.hasJieQi })}>
+                  {cell.displayText}
+                </span>
+              </button>
+            </Tooltip>
+          </Fragment>
         );
       })}
     </div>
diff --git a/src/components/settings/forms/CalendarForm.tsx b/src/components/settings/forms/CalendarForm.tsx
index 37ef17f..5ec6835 100644
--- a/src/components/settings/forms/CalendarForm.tsx
+++ b/src/components/settings/forms/CalendarForm.tsx
@@ -1,4 +1,4 @@
-import { Form, Switch } from 'antd';
+import { Form, Segmented, Switch } from 'antd';
 import React from 'react';
 import { syncValuesConfig } from '../../../sync/base/syncValuesConfig.ts';
 import { useConfigSync } from '../../../sync/configStore.ts';
@@ -15,6 +15,19 @@ const CalendarForm: React.FC = () => {
       initialValues={config}
       onValuesChange={syncValuesConfig}
     >
+      <Form.Item name="calendarWeekNumberVisible" label="显示每周周数">
+        <Switch />
+      </Form.Item>
+      {config.calendarWeekNumberVisible && (
+        <Form.Item name="calendarWeekNumberStart" label="1月1日所在周">
+          <Segmented
+            options={[
+              { label: '第0周', value: 0 },
+              { label: '第1周', value: 1 },
+            ]}
+          />
+        </Form.Item>
+      )}
       <Form.Item name="calendarFooterVisible" label="显示底部信息区域">
         <Switch />
       </Form.Item>
diff --git a/src/hooks/calender/useCalendarViewModel.ts b/src/hooks/calender/useCalendarViewModel.ts
index 9a3b84a..37b5477 100644
--- a/src/hooks/calender/useCalendarViewModel.ts
+++ b/src/hooks/calender/useCalendarViewModel.ts
@@ -102,6 +102,8 @@ export const useCalendarViewModel = (): CalendarViewModel => {
   const { data: config, sync: syncConfig } = useConfigSync();
   const {
+    calendarWeekNumberVisible,
+    calendarWeekNumberStart,
     calendarFooterVisible,
     footerFestivalVisible,
     footerYiJiVisible,
@@ -130,6 +132,7 @@ export const useCalendarViewModel = (): CalendarViewModel => {
     transparent: effectiveTransparent,
     isDark,
     backgroundOpacity: effectiveBackgroundOpacity,
+    weekNumbersVisible: calendarWeekNumberVisible,
   });
 
   // 选中哪一天、月历面板显示哪个月（可不同步，例如选其它月中的日期会跳面板）
@@ -312,6 +315,8 @@ export const useCalendarViewModel = (): CalendarViewModel => {
   const gridProps: CalendarMonthGridProps = {
     styles,
     cellModels,
+    showWeekNumbers: calendarWeekNumberVisible,
+    firstWeekNumber: calendarWeekNumberStart,
     onSelectDate: handleSelectDate,
   };
 
diff --git a/src/styles/calendarView/gridStyles.ts b/src/styles/calendarView/gridStyles.ts
index 7cbf5e5..2a2b2b0 100644
--- a/src/styles/calendarView/gridStyles.ts
+++ b/src/styles/calendarView/gridStyles.ts
@@ -5,11 +5,50 @@ export const createGridStyles = (css: CSSBuilder): CalendarViewStyles['grid'] =>
   return {
     calendarGrid: css({
       display: 'grid',
-      gridTemplateColumns: 'repeat(7, 1fr)',
+      gridTemplateColumns: 'repeat(7, minmax(0, 1fr))',
       gridTemplateRows: 'auto repeat(6, minmax(var(--calendar-grid-row-min-height), 1fr))',
       gap: 'var(--calendar-grid-gap)',
       flex: 1,
       minHeight: 0,
     }),
+    calendarGridWithWeekNumbers: css({
+      gridTemplateColumns: 'var(--calendar-week-number-column-width) repeat(7, minmax(0, 1fr))',
+    }),
+    weekNumberHeader: css({
+      minWidth: 0,
+      textAlign: 'center',
+      color: theme.subText,
+      fontSize: 'var(--calendar-weekday-font-size)',
+      lineHeight: 'var(--calendar-weekday-line-height)',
+      fontWeight: 500,
+      display: 'flex',
+      alignItems: 'center',
+      justifyContent: 'center',
+      userSelect: 'none',
+    }),
+    weekNumberCell: css({
+      minWidth: 0,
+      borderRadius: 'var(--calendar-radius)',
+      display: 'flex',
+      alignItems: 'center',
+      justifyContent: 'center',
+      color: theme.subText,
+      background: theme.navButtonBackground,
+      fontSize: 'var(--calendar-week-number-font-size)',
+      fontWeight: 600,
+      lineHeight: 1,
+      fontVariantNumeric: 'tabular-nums',
+      userSelect: 'none',
+      whiteSpace: 'nowrap',
+      '@media (max-width: 520px)': {
+        fontSize: 'calc(var(--calendar-week-number-font-size) - 1px)',
+      },
+    }),
     weekday: css({
       minWidth: 0,
       textAlign: 'center',
diff --git a/src/styles/calendarView/shellStyles.ts b/src/styles/calendarView/shellStyles.ts
index 9b2b13b..886f564 100644
--- a/src/styles/calendarView/shellStyles.ts
+++ b/src/styles/calendarView/shellStyles.ts
@@ -7,6 +7,10 @@ export const createShellStyles = (
   const { theme, runtime } = input;
   const { transparent, isDark, backgroundOpacity } = runtime;
   const panelBackground = theme.panelBackground;
+  const calendarBaseWidth = runtime.isMobile ? 320 : 400;
+  const calendarWidth =
+    calendarBaseWidth + (runtime.weekNumbersVisible ? (runtime.isMobile ? 34 : 42) : 0);
 
   return {
     micaContainer: css({
@@ -15,7 +19,7 @@ export const createShellStyles = (
       gap: 'var(--calendar-shell-gap)',
       padding: 'var(--calendar-shell-padding)',
       height: runtime.isMobile ? '100%' : 620,
-      width: runtime.isMobile ? '100%' : 400,
+      width: runtime.isMobile ? '100%' : calendarWidth,
       maxWidth: '100%',
       color: theme.text,
       background: transparent ? 'transparent' : panelBackground,
diff --git a/src/styles/calendarView/types.ts b/src/styles/calendarView/types.ts
index 45554d7..fa04b36 100644
--- a/src/styles/calendarView/types.ts
+++ b/src/styles/calendarView/types.ts
@@ -42,6 +42,9 @@ export interface CalendarStyleRuntime {
   transparent: boolean;
   isDark: boolean;
   backgroundOpacity: number;
+  /** 月历左侧是否显示周数列；桌面窗口据此适当加宽。 */
+  weekNumbersVisible: boolean;
 }
 
 export interface CalendarHeaderStyleInput {
@@ -61,10 +64,13 @@ export interface CalendarGridStyleInput {
 export interface CalendarGridStyles {
   calendarGrid: string;
+  calendarGridWithWeekNumbers: string;
   weekday: string;
+  weekNumberHeader: string;
+  weekNumberCell: string;
   cell: string;
   otherMonth: string;
   selected: string;
diff --git a/src/styles/calendarShellVariables.css b/src/styles/calendarShellVariables.css
index 4b10f62..48c2cf0 100644
--- a/src/styles/calendarShellVariables.css
+++ b/src/styles/calendarShellVariables.css
@@ -11,6 +11,8 @@
   --calendar-grid-row-min-height: 68px;
   --calendar-weekday-font-size: 14px;
   --calendar-weekday-line-height: 20px;
+  --calendar-week-number-column-width: 36px;
+  --calendar-week-number-font-size: 12px;
   --calendar-cell-date-font-size: 16px;
   --calendar-cell-date-line-height: 22px;
   --calendar-cell-lunar-font-size: 12px;
diff --git a/src/styles/useCalendarViewStyles.ts b/src/styles/useCalendarViewStyles.ts
index 6ae3077..22186c0 100644
--- a/src/styles/useCalendarViewStyles.ts
+++ b/src/styles/useCalendarViewStyles.ts
@@ -13,6 +13,7 @@ export interface UseCalendarViewStylesInput {
   transparent: boolean;
   isDark: boolean;
   backgroundOpacity: number;
+  weekNumbersVisible: boolean;
 }
 
 /** CalendarView 主样式 hook：只负责根据主题/运行态组装各 section 样式。 */
@@ -28,6 +29,7 @@ export const useCalendarViewStyles = createStyles((input: UseCalendarViewStylesIn
     transparent,
     isDark,
     backgroundOpacity,
+    weekNumbersVisible,
   } = input;
 
   const theme = computeCalendarTheme(isDark);
@@ -39,6 +41,7 @@ export const useCalendarViewStyles = createStyles((input: UseCalendarViewStylesIn
       transparent,
       isDark,
       backgroundOpacity,
+      weekNumbersVisible,
     },
   };
 
diff --git a/src/sync/configStore.ts b/src/sync/configStore.ts
index 08a082b..0a7aef4 100644
--- a/src/sync/configStore.ts
+++ b/src/sync/configStore.ts
@@ -15,6 +15,8 @@ const systemConfigDefaults = {
 } satisfies SystemConfig;
 
 const calendarFooterVisibleDefaults = {
+  calendarWeekNumberVisible: true,
+  calendarWeekNumberStart: 1,
   calendarFooterVisible: true,
   footerFestivalVisible: true,
   footerYiJiVisible: false,
diff --git a/src/sync/type/configTypes.ts b/src/sync/type/configTypes.ts
index 71ad822..90bb11d 100644
--- a/src/sync/type/configTypes.ts
+++ b/src/sync/type/configTypes.ts
@@ -1,4 +1,5 @@
 import type { TrayClockDateFormat, TrayClockTimeFormat } from '../../enums/trayClockEnum.ts';
+import type { CalendarWeekNumberStart } from '../../utils/calendar/calendarWeekNumber.ts';
 
 export type MacosVibrancyEffect =
   | 'blur'
@@ -35,6 +36,10 @@ export interface SystemConfig {
 }
 
 export interface CalendarFooterVisible {
+  /** 在月历左侧显示每一行的年度周数 */
+  calendarWeekNumberVisible: boolean;
+  /** 包含 1 月 1 日的第一周显示为 W0 或 W1 */
+  calendarWeekNumberStart: CalendarWeekNumberStart;
   /** 显示底部信息区域 */
   calendarFooterVisible: boolean;
   /** 显示节日信息 */
diff --git a/src/utils/calendar/calendarWeekNumber.ts b/src/utils/calendar/calendarWeekNumber.ts
new file mode 100644
index 0000000..0e04a6d
--- /dev/null
+++ b/src/utils/calendar/calendarWeekNumber.ts
@@ -0,0 +1,43 @@
+import type { Dayjs } from 'dayjs';
+
+/** 年度首周可从 0 或 1 开始编号。 */
+export type CalendarWeekNumberStart = 0 | 1;
+
+/**
+ * 返回某日期所在“周行”的周一。
+ * 月历表头固定为周一至周日，因此周数也按周一切换。
+ */
+export function getMondayOfWeek(date: Dayjs): Dayjs {
+  const daysSinceMonday = (date.day() + 6) % 7;
+  return date.startOf('day').subtract(daysSinceMonday, 'day');
+}
+
+/**
+ * 计算月历左侧显示的年度周序号。
+ *
+ * 规则：
+ * - 每周从周一开始；
+ * - 包含 1 月 1 日的那一整行是新年度的第一周；
+ * - 用户可选择该行显示为 W0 或 W1；
+ * - 之后每过一个周一递增 1；
+ * - 若一行跨年并包含下一年的 1 月 1 日，则该行立即按新年度重新编号。
+ */
+export function getCalendarRowWeekNumber(
+  dateInRow: Dayjs,
+  firstWeekNumber: CalendarWeekNumberStart,
+): number {
+  const rowStart = getMondayOfWeek(dateInRow);
+  const rowEnd = rowStart.add(6, 'day');
+  // 只有跨年周才会出现 rowStart.year() !== rowEnd.year()；该周包含新年的 1 月 1 日，
+  // 因而应按新年度编号。
+  const weekYear = rowEnd.year();
+  const jan1 = rowEnd.startOf('year');
+  const firstRowStart = getMondayOfWeek(jan1);
+  const civilDayNumber = (date: Dayjs): number =>
+    Date.UTC(date.year(), date.month(), date.date()) / 86_400_000;
+  const weekOffset = (civilDayNumber(rowStart) - civilDayNumber(firstRowStart)) / 7;
+
+  return firstWeekNumber + weekOffset;
+}
